#include<stdio.h>
#include <stdlib.h>
#include <string.h>

void main()
{
    /*
       FILE *f;
       char ch;
       f = open("hello.txt",'r');
       while(ch!=EOF)
       {
           ch = fgetc(f);
           printf("%c",ch);
       }
       fclose(f);
    */
    /*char cc = (char*)malloc(sizeof(char)*5);
    char *lac,*no;
    char *ch;
    int i = 0;
    ch = getchar();
    printf("%s",ch);
//    strstr();
    while(*ch!='-')
    {
        cc[i] = *ch++;
        i++;
    }
    cc[i]='\0';
    printf("%s",cc);
    */

char ch;
ch = getchar();
printf("%d",ch);
}
